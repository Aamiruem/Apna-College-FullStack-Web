for(let i = 1; i>=0; i++){
    console.log(i);
}


// this is an infinite loop because i is always greater than or equal to 0 and it never gets incremented so it will keep on printing the value of i.
// this is called an infinite loop and it will keep on running until the system crashes.    
// so always make sure that the condition in the loop is always true and it should be updated so that it can break the loop.
for(let i = 1; i<=5; i--){
    console.log(i);
}




// this is also an infinite loop because i is always greater than or equal to 0 and it never gets incremented so it will keep on printing the value of i.
// this is called an infinite loop and it will keep on running until the system crashes.
// so always make sure that the condition in the loop is always true and it should be updated so that it can break the loop.
for(let i = 1; ; i++){
    console.log(i);
}
