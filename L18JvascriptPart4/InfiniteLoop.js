// for(let i = 1; i>=0; i++){
//     console.log(i);
// }


// this is an infinite loop because i is always greater than or equal to 0 and it never gets incremented so it will keep on printing the value of i.
// this is called an infinite loop and it will keep on running until the system crashes.
// so always make sure that the condition in the loop is always true and it should be updated so that it can break the loop.

// for(let i = 1; i<=5; i--){
//     console.log(i);
// }




// this is also an infinite loop because i is always greater than or equal to 0 and it never gets incremented so it will keep on printing the value of i.
// this is called an infinite loop and it will keep on running until the system crashes.
// so always make sure that the condition in the loop is always true and it should be updated so that it can break the loop.

// for(let i = 1; ; i++){
//     console.log(i);
// }



for(let i = 1; i>1; i++){
    console.log(i);
}
i = 1; // this is the initialization of the loop and it will be executed only once when the loop is created.

// i>1; // this is the condition of the loop and it will be checked every time the loop is executed. if it is true then the loop will be executed otherwise it will be terminated.

// i++; // this is the updation of the loop and it will because it will be executed every time the loop is executed. if it is true then the loop will be executed otherwise it will be terminated.

// this is the body of the loop and it will be executed every time the loop is executed. if it is true then the loop will be executed otherwise it will be terminated.
